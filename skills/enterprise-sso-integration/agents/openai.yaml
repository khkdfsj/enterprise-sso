interface:
  display_name: "ESSO-DFSJ Integration"
  short_description: "Integrate internal apps with the ESSO-DFSJ package"
  default_prompt: "Use $enterprise-sso-integration to integrate this application with the generated ESSO-DFSJ package and complete login, logout, and connectivity checks."
