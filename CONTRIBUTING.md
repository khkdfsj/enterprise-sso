# 开发与发布约定

1. GitHub 是唯一源码基线，禁止长期保留仅存在于生产服务器的修改。
2. 每项功能同时更新测试和对应中文手册。
3. 提交前运行 `npm test`、`npm run check`；认证或换届逻辑还要运行对应端到端测试。
4. 不提交环境变量、数据库、私钥、人员数据、日志和备份。
5. 生产发布必须能对应唯一 commit 和 tag，并保留前一 release 与数据库回滚点。
6. 114/118 上有其他程序；部署只能操作本项目明确列出的目录、端口和 systemd 服务。
