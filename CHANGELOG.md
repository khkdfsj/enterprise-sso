# 版本记录

## 0.4.0 - 2026-09-01

- 系统对外简称统一为 ESSO，固定接入包和目录名为 `ESSO-DFSJ`。
- 新增一次性 ZIP 接入包：用户只填项目根地址，系统自动生成并登记回调、注销、健康和验收地址。
- 接入包包含配置、协议客户端、登录与身份读取、回调、注销、健康检查和两项验收文件。
- 新增部长及以上职级、老师和后台管理员服务删除权限、独立二次确认页、级联清理和删除审计。
- 管理后台所有时间统一按 Asia/Shanghai 显示，`datetime-local` 输入也明确按北京时间解析。
- 接入文档补充企业微信与 OIDC 技术链路、完整术语表、文件说明、代码实例、安全要求和故障排查。

## 0.3.0 - 2026-09-01

- 管理后台自身改为内部 OIDC 客户端，密码和企业微信扫码统一走托管登录页。
- 后台重组为接入服务管理、部门人员管理、系统管理三个一级模块及二级菜单。
- 新增四步接入向导，自动生成登录、回调、注销、连通检测与验收代码。
- 新增签名连通检测、十分钟持续监控、真实登录认证测试和统一注销回跳测试。
- 服务详情改为概览、登录与注销端点、访问权限、密钥与注册、连通监控二级页。
- 增加统一会话列表和管理员强制注销能力。
- 企业微信桌面二维码改用独立可信域名入口，不再直接编码 IP 地址。
- PHP 7.4 SDK 注销改为清理本地会话后调用 OIDC `end_session_endpoint`。

## 0.2.0 - 2026-08-31

- 统一使用企业微信 UserID/学号作为人员主键，并阻止不一致的企业微信绑定。
- 增加网页管理后台、永久人员、人员状态历史和年度换届暂停/恢复。
- 增加业务系统发起的短期快捷注册，密码只由认证中心接收。
- 增加 DepartmentIFO 只读导出、冲突预演、带指纹迁移、验证和批次回滚。
- 以真实 181 快照验证 120 个唯一 UserID，并识别三名永久超级管理员。
- 增加可供其他 Agent 使用的标准 Enterprise SSO 接入 Skill。
- 允许管理员逐个批准内网 HTTP 回调主机，不改变其他系统的 HTTP/HTTPS 行为。

## 0.1.0 - 2026-08-30

- 建立全新 Enterprise SSO，不复用旧认证系统代码和数据库。
- 支持密码认证、企业微信扫码事务、OIDC Authorization Code + PKCE。
- 支持有限时 SSO、动态应用准入和权限版本。
- 建立人员、届次、任职与原子换届模型。
- 提供 PHP 7.4 一行守卫 SDK。
- 部署 114 独立 8443 HTTPS 入口和受限隧道，118 后端仅监听回环地址。
- 明确禁用 HSTS，避免影响同 IP 的原有 HTTP 系统。
- 增加内部 CA、证书自动续期检查和完整运维文档。
